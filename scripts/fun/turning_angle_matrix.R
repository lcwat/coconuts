# turning angle matrix
# this script will find the turning angle between an approach angle to an object
# and all other objects in a given 2D space

turning_angle_matrix <- function(previous_x, previous_y, current_x, current_y, available_objects_df) {
  
}